# Frontend (Vite) — copy to .env.local for local dev
# On Vercel: set in Project → Settings → Environment Variables

# FastAPI backend URL (Render in production)
VITE_API_BASE_URL=http://127.0.0.1:8000
